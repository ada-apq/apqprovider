--
-- author Marcelo Coraça de Freitas <marcelo.batera@gmail.com> 
--
-- Repository information:
-- $Date: 2008-02-25 19:47:48 -0300 (Seg, 25 Fev 2008) $
-- $Revision: 234 $
-- $Author: ogro $


-- setup for the build system...
with "kow_framework_setup";


-- database :)
with "apq";

-- configuration framework
with "kowconfig";
with "kowlib";

project APQ_Provider is

	version	:= $version;
	prefix	:= $prefix;
	
	for Source_Dirs use ( prefix & "/include" );
	
	-- LIBRARY
	for Library_Dir use prefix & "/lib";
	for Library_Name use "apq_provider";
	for Library_Version use "libapq_provider.so." & Version;
	for Library_Kind use kow_framework_Setup'Library_Kind;
	
	
	package Compiler is
		for Default_Switches ("ada") use ("-O2", "-gnat05");
	end Compiler;

end APQ_Provider;

